version: 1
common:
  resourcesPath: ../resources
  logLevel: info
  appLogDestination: console
  daprdLogDestination: console

apps:
  - appID: support-workflow
    appDirPath: ./
    appPort: 8001
    appProtocol: http
    command: [ "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8001" ]
